import GoldDripDivider from './GoldDripDivider';

// Re-exported as a gold-drip divider replacing the previous organic wave.
// `color` and `flip` props from the old API are accepted but no longer used.
export default function SectionDivider(_props) {
  return <GoldDripDivider />;
}
